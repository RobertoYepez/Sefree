<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sefree">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sefree">
        <activity
            android:name=".PoliticasYCondiciones"
            android:exported="true" />
        <activity
            android:name=".ClasificacionMovimiento"
            android:exported="true" />
        <activity
            android:name=".DetalleMovimientos"
            android:exported="true" />

        <activity
            android:name=".movimientos"
            android:exported="true" />
        <activity
            android:name=".RecupClave"
            android:exported="true" />

        <activity android:name=".TipodeMovimiento" />
        <activity android:name=".CreacionSefree" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>